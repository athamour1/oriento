services:
  db:
    image: postgres:15-alpine
    container_name: orienteering_db_prod
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: orienteering
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data

  backend:
    image: ghcr.io/athamour1/orientiring/backend:latest
    container_name: orienteering_backend_prod
    restart: always
    depends_on:
      - db
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://admin:admin123@db:5432/orienteering?schema=public
      - JWT_SECRET=change_this_to_a_secure_random_string!
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin123
      - CORS_ORIGIN=* # Change to http://your-domain.com in real production
      - DEFAULT_LANGUAGE=el

  frontend:
    image: ghcr.io/athamour1/orientiring/frontend:latest
    container_name: orienteering_frontend_prod
    restart: always
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=http://localhost:3000  # Change to your production backend URL

volumes:
  postgres_data_prod:
